spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:///C:/git%20repo/udemy-spring-cloud-microservices-configurations/

server:
  port: 8888
